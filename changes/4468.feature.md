A ``white_space`` style property was added, allowing ``Label`` text to wrap to the width it is allocated (height-for-width layout) instead of overflowing on a single line. Wrapping is currently implemented on the GTK and Android backends.
